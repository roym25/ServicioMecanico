---
// src/layouts/MainLayout.astro
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Taller mecánico profesional. Servicio automotriz de confianza.' } = Astro.props;
---

<!doctype html>
<html lang="es" class="h-full scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body class="min-h-full flex flex-col font-sans antialiased text-gray-100 relative bg-[#111] bg-fixed bg-cover bg-center" style="background-image: url('/fondoindex.jpg');">
    <!-- Capa de oscurecimiento global (overlay) para asegurar lectura -->
    <div class="fixed inset-0 bg-black/60 z-[-1]"></div>
    <Header />
    <main class="flex-grow">
      <slot />
    </main>
    <Footer />
  </body>
</html>
